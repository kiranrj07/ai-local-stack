# Aider config — HYBRID profile. Routes inference to Ubuntu worker via SSH tunnel.
# Start the tunnel first: bash scripts/start_remote_ollama_tunnel.sh

model: ollama_chat/qwen2.5-coder:14b
weak-model: ollama_chat/qwen2.5-coder:7b
api-base: http://localhost:11435
# Direct LAN alternative (less safe — see docs/12-dual-mode-mac-ubuntu.md):
# api-base: http://REPLACE_ME_WORKER_LAN_IP:11434

analytics: false
gitignore: true
auto-commits: false
dirty-commits: false
